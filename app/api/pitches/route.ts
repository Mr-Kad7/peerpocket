import {NextResponse} from 'next/server'; import {getSupabaseAdmin} from '@/lib/supabaseAdmin';
export async function POST(req:Request){try{const b=await req.json();const reqd=['title','entrepreneur_name','category','goal','story','use_of_funds'];if(reqd.some(k=>!b[k]))return NextResponse.json({error:'Please complete all required fields.'},{status:400});const {error}=await getSupabaseAdmin().from('pitch_submissions').insert({title:b.title,entrepreneur_name:b.entrepreneur_name,category:b.category,goal:Number(b.goal),story:b.story,use_of_funds:b.use_of_funds,location:b.location||null,image_url:b.image_url||null,status:'review'});if(error)throw error;return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Pitch submission could not be saved. Configure Supabase first.'},{status:503})}}
